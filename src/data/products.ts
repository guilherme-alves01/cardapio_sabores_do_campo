import type { Product } from '../types';

/**
 * Os produtos agora são gerenciados exclusivamente via Supabase.
 * Este arquivo permanece vazio para evitar dependências quebradas,
 * mas a lista de produtos local foi migrada para o banco de dados.
 */

export const products: Product[] = [];
